<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.SyncClipboard" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/transparent</item>
    </style>

    <!-- 用于 悬浮窗 模式的宿主 Activity：全屏透明、不接管触摸，仅用于保持前台态 -->
    <style name="Theme.SyncClipboard.FloatingHost" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimAmount">0.0</item>
    </style>

    <!-- 用于 Tile 触发的剪贴板桥接 Activity：全屏透明，仅用于短暂获取焦点读取剪贴板 -->
    <style name="Theme.SyncClipboard.TransparentHost" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimAmount">0.0</item>
    </style>
</resources>
